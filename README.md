# multi-agent

Framer / SQLBuilder / Finisher の3エージェントで、依頼を BigQuery SQL に落とし、
runs/ に成果物を保存する。

## Quickstart
- `cp .env.example .env` して `MODEL=...` を設定
- `python -m src.run "依頼文"`

## SoT
- PROJECT_CONTEXT.md
- STATE.md
